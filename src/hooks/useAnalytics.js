import { useEffect } from 'react'\nimport { CAMPAIGN_CONFIG } from '../utils/constants'\n\n/**\n * Hook personalizado para tracking de eventos de campaña\n * Implementa principios SOLID: Single Responsibility\n */\nexport const useAnalytics = () => {\n  \n  const trackEvent = (eventName, parameters = {}) => {\n    // Google Analytics 4\n    if (typeof gtag !== 'undefined') {\n      gtag('event', eventName, {\n        ...parameters,\n        campaign_type: 'muebles_digital',\n        customer_segment: 'millennials_centennials'\n      })\n    }\n    \n    // Facebook Pixel\n    if (typeof fbq !== 'undefined') {\n      fbq('track', eventName, parameters)\n    }\n    \n    // Console para desarrollo\n    console.log('🎯 Analytics Event:', eventName, parameters)\n  }\n  \n  const trackPageView = (pageName, category = null) => {\n    trackEvent('page_view', {\n      page_title: pageName,\n      page_category: category,\n      customer_journey_stage: getJourneyStage(pageName)\n    })\n  }\n  \n  const trackConversion = (goalType, value = 0) => {\n    trackEvent('conversion', {\n      goal_type: goalType,\n      conversion_value: value,\n      campaign_attribution: 'muebles_homepage'\n    })\n  }\n  \n  const trackUserEngagement = (interactionType, element) => {\n    trackEvent('user_engagement', {\n      interaction_type: interactionType,\n      element_id: element,\n      engagement_time: Date.now()\n    })\n  }\n  \n  const getJourneyStage = (pageName) => {\n    if (pageName === 'home') return 'awareness'\n    if (pageName.includes('category')) return 'consideration'\n    if (pageName.includes('product')) return 'decision'\n    return 'exploration'\n  }\n  \n  return {\n    trackEvent,\n    trackPageView,\n    trackConversion,\n    trackUserEngagement\n  }\n}